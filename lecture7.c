// System call stat() helps access a files inode
// S_ISREG(), S_ISDIR() and more macros in presentation 5 should help determine file type
// an example is in page 30 in ipad
// lstat should be uses, in case we encounter a soft link
